<header class="bg-dark py-5">
    <div class="container px-4 px-lg-5">
        <div class="text-center text-white">
            <h1 class="display-4 fw-bolder">Receitas</h1>
            <p class="lead fw-normal text-white-50 mb-0">Crie ou encontre uma receita</p>

            <%= render "partials/search" %>
        </div>
    </div>
</header>
<section class="py-5">
    <div class="row col-lg-3">
        <div class="text-center">
            <%= "Não possuimos nenhum receita cadastrada no momento." if @recipes.empty? %>
        </div>
        <% @recipes.each do |recipe| %>
            <div class="col">
                <div class="card h-100">
                    <img src=<%= ["https://cdn0.tudoreceitas.com/pt/posts/6/8/1/sobremesa_para_domingo_de_pascoa_9186_600.webp", 
                    "https://cdn0.tudoreceitas.com/pt/posts/1/8/3/bolinho_de_arroz_sem_farinha_11381_600.webp", 
                    "https://cdn0.tudoreceitas.com/pt/posts/9/9/7/panqueca_de_carne_moida_fit_10799_600.webp"].sample %> class="card-img-top"
                    alt="" />
                    <div class="card-body">
                        <h5 class="card-title font-weight-bold"><%= recipe.name %></h5>
                        <p class="card-text">
                            <%= recipe.body %>
                        </p>
                    </div>
                    <div class="card-footer">
                        <%= link_to recipe, {class: 'text-primary btn-sm text-decoration-none' }  do %>
                            <i class="fa fa-eye" title="Ver detalhes"></i>VER
                        <% end %>
                        <%= link_to edit_recipe_path(recipe), {class: 'text-warning btn-sm text-decoration-none'} do %>
                            <i class="fa fa-edit" title="Editar"></i>EDITAR
                        <% end %>
                        <%= link_to "DELETAR", recipe_path(recipe), data: {
                            turbo_method: :delete,
                            turbo_confirm: "Tem certeza que deseja excluir este item?" },
                            class: 'text-danger btn-sm text-decoration-none' %>
                    </div>
                </div>
            </div>
        <% end %>
    </div>
</section>

